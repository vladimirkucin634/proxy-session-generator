# Proxy Session Generator

This Python project creates HTTP sessions with rotating proxy support and retry logic using `requests`.

## Features

- Automatically fetches free proxies.
- Builds robust sessions with retry strategies.
- Logs selected proxy for each session.

## Setup

```bash
pip install -r requirements.txt
```
